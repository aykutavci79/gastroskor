/** Keşfet vitrin — sipariş / rezervasyon / yöresel banner metin boyutları (hepsi aynı). */
export const KESFET_VITRIN_BANNER = {
  /** Online Sipariş / Rezervasyon / Yöresel Lezzetler pill */
  pillFontSize: 18,
  /** "Tek Tıkla Liste / Masa / Keşfet" — pill alt satırı */
  titleFontSize: 18,
  hintFontSize: 14,
  /** Carousel slayt etiketi (ürün adı, sahne) */
  slideTagFontSize: 11,
  iconSize: 24,
  iconCircle: 48,
  minHeight: 58,
} as const;

export const KESFET_VITRIN_TEXT_SHADOW = {
  textShadowColor: 'rgba(0,0,0,0.75)',
  textShadowOffset: { width: 0, height: 1 },
  textShadowRadius: 5,
} as const;
